# Leakage and test-key boundaries

Answer-shaped fields such as expected coordinates, correct indexes, hold times, labels, or server answers may be recorded as leakage observations. They must not enter the prediction process.

Provider test keys, deterministic dummy tokens, and official demos are useful for integration and negative-control fixtures. They remain `boundary_only` and cannot approve a production or partner-target capability.
