# Dataset provenance and split contract

Each sample records acquisition mode, authorization scope, source run, source asset and content hash, challenge lineage, template or generation family, label source, synthetic status, and redaction state.

Split assignment operates on groups, not sample IDs alone. Exact and near-duplicate content, templates, sessions, and challenge lineages must not cross train, validation, or test boundaries.
