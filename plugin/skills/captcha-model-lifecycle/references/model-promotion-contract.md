# Model promotion contract

A model is eligible for scoped promotion only when its package checksum, checkpoint checksum, dataset and split hashes, code/config versions, load smoke, inference smoke, holdout metrics, family gates, leakage audit, raster-only gate, and failure report all pass.

Promotion remains scoped to the evaluated challenge families and environment. It cannot create an action, provider, or business acceptance capability.
